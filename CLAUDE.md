# Weekkeep Claude Code Context

Read and follow `AGENTS.md` completely before making changes. The canonical product context and all decision values/statuses start at the Decision Registry in `docs/00-INDEX.md`; the complete documentation map is in `README.md`, and the current visual baseline starts at `design/README.md`.

The repository is in Implementation against the approved 0.5 documentation baseline. Create production code only within that scope, keep traceability evidence current, and do not infer new V1 scope from the legacy Peeka repository or prior conversations; propose product changes in the canonical documents first.
